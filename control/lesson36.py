
# ! Docstrings


def example_func(params1, params2):
    """Example function with types documented in the docstrings.

    Args:
        params1 (int): The first parameter.
    """
    print(params1)


print(example_func.__doc__)

# ? Example function with types documented in the docstrings.

# ?    Args:
# ?        params1 (int): The first parameter.

# 関数内で書いた、関数の説明を呼び出すことが出来る。
